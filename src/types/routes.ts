export const enum MainRoutes {
  Main = "/",
}

export const enum ReviewsRoutes {
  Get = "/reviews",
  GetById = "/reviews/:id",
  Create = "/reviews/create",
  Update = "/reviews/update/:id",
  Remove = "/reviews/delete/:id",
}
